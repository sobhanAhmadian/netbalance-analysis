# Netbalance:

a Systematic Evaluation Framework For Fair Assessment of Bipartite Graph Link Prediction.

## Setting Up the Project

1. Create a virtual environment for the project: `python -m venv .venv` and activate it
2. Run `python -m pip install -r requirements.txt`
3. Install the `netbalance` package via `python -m pip install -e .`